@model MISAF_Project.Controllers.Test

<form id="modalForm" action="@Url.Action("Edit", "Home")" method="post">
    @*@Html.HiddenFor(m => m.ID_No)*@
    <div class="form-group">
        <label>Name</label>
        @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
    </div>
    <button type="submit" class="btn btn-success">Save</button>
</form>

